# Production stack (docs/deployment.md). Standalone — never merged with the local file:
#
#   docker compose -f docker-compose.production.yaml up -d --build
#
# Caddy is the only service with published ports; api, worker, web and pg are reachable
# on the compose network alone. Secrets come from `.env` next to this file.

services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    environment:
      # Both are read by the Caddyfile; see the production block of .env.example.
      DOMAIN: ${DOMAIN}
      ACME_EMAIL: ${ACME_EMAIL}
    volumes:
      - ./deploy/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - api
      - web

  pg:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-sightread}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:-sightread}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-sightread} -d ${POSTGRES_DB:-sightread}"]
      interval: 10s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ./apps/api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      APP_ENV: production
      UPLOAD_DIR: /data/uploads
      FIGURES_DIR: /data/figures
    depends_on:
      pg:
        condition: service_healthy
    volumes:
      - uploads:/data/uploads
      - figures:/data/figures
    # Migrations run once, from the api container, before it serves (docs/database.md).
    command: >
      sh -c "alembic upgrade head &&
      uvicorn sightread.main:app --host 0.0.0.0 --port 8000"
    # Healthy only once /healthz answers — which is only after `alembic upgrade head`
    # finished and uvicorn is serving. The worker gates on this, so it can never claim a
    # queued job against a schema the migration has not written yet.
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/healthz', timeout=3)"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 60s

  worker:
    build:
      context: ./apps/api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      APP_ENV: production
      UPLOAD_DIR: /data/uploads
      FIGURES_DIR: /data/figures
    depends_on:
      pg:
        condition: service_healthy
      # service_healthy, not service_started: started only means the container exists,
      # and a worker that claims a job mid-migration bills the user and then fails on
      # the missing column.
      api:
        condition: service_healthy
    volumes:
      - uploads:/data/uploads
      - figures:/data/figures
    command: ["python", "-m", "sightread.worker"]

  web:
    build:
      context: ./apps/web
    restart: unless-stopped
    depends_on:
      - api

  backup:
    # Leaving a managed platform means backups are ours (docs/deployment.md). One pass a
    # day into ./backups — the database dump plus an archive of the stored figure crops,
    # which are durable result data a restored database cannot recreate — keeping the
    # newest seven of each. Deliberately a visible shell loop: a cron container would hide
    # the schedule and the rotation in two more places.
    image: postgres:16
    restart: unless-stopped
    environment:
      PGUSER: ${POSTGRES_USER:-sightread}
      PGPASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      PGDATABASE: ${POSTGRES_DB:-sightread}
      PGHOST: pg
    volumes:
      - ./backups:/backups
      - figures:/data/figures:ro
    depends_on:
      pg:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -eu
        while true; do
          stamp=$$(date -u +%Y%m%dT%H%M%SZ)
          if pg_dump --format=custom --file="/backups/sightread-$$stamp.dump.tmp"; then
            mv "/backups/sightread-$$stamp.dump.tmp" "/backups/sightread-$$stamp.dump"
            echo "backup: wrote sightread-$$stamp.dump"
          else
            rm -f "/backups/sightread-$$stamp.dump.tmp"
            echo "backup: pg_dump failed at $$stamp" >&2
          fi
          # The crops behind the dumped results; restore untars this back into the volume.
          if tar -czf "/backups/figures-$$stamp.tar.gz.tmp" -C /data figures; then
            mv "/backups/figures-$$stamp.tar.gz.tmp" "/backups/figures-$$stamp.tar.gz"
            echo "backup: wrote figures-$$stamp.tar.gz"
          else
            rm -f "/backups/figures-$$stamp.tar.gz.tmp"
            echo "backup: figures archive failed at $$stamp" >&2
          fi
          # Keep the newest seven of each; anything older is deleted.
          ls -1t /backups/sightread-*.dump 2>/dev/null | tail -n +8 | while read -r old; do
            rm -f "$$old"
            echo "backup: rotated out $$old"
          done
          ls -1t /backups/figures-*.tar.gz 2>/dev/null | tail -n +8 | while read -r old; do
            rm -f "$$old"
            echo "backup: rotated out $$old"
          done
          sleep 86400
        done

volumes:
  pgdata:
  uploads:
  figures:
  caddy_data:
  caddy_config:
